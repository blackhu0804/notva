{
  "name": "notva",
  "version": "0.0.0",
  "type": "module",
  "bin": {
    "notva": "./dist/main.js"
  },
  "dependencies": {
    "@notva/core": "workspace:*",
    "@notva/server": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run src"
  }
}

{
  "name": "notva",
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",
  "bin": {
    "notva": "./dist/main.js"
  },
  "dependencies": {
    "@notva/core": "workspace:*",
    "@notva/server": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run src"
  }
}
